---
phase: 01-core-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - core/errors.ts
  - core/types.ts
  - core/config.ts
  - package.json
  - test/core/errors.test.ts
  - test/core/config.test.ts
autonomous: true
requirements:
  - CODE-01
  - CODE-04

must_haves:
  truths:
    - "bun run typecheck passes with zero errors (typescript installed as devDependency)"
    - "catching InstitutionalError catches ProviderError, ValidationError, and FrameworkError"
    - "every error carries machine-readable code, framework context, and cause chain"
  artifacts:
    - path: "core/errors.ts"
      provides: "Typed error hierarchy"
      exports: ["InstitutionalError", "ProviderError", "ValidationError", "FrameworkError", "ErrorCode", "ErrorContext"]
    - path: "test/core/errors.test.ts"
      provides: "Error hierarchy tests"
      min_lines: 40
  key_links:
    - from: "core/errors.ts"
      to: "InstitutionalError"
      via: "class inheritance chain"
      pattern: "extends InstitutionalError"
---

<objective>
Install TypeScript as explicit devDependency so `bun run typecheck` resolves `tsc`, and create the typed error hierarchy that all subsequent plans depend on.

Purpose: CODE-01 unblocks typecheck verification for every future task. CODE-04 creates the error types that retry/timeout/provider code will throw.
Output: `core/errors.ts` with full hierarchy, passing tests, `bun run typecheck` green.
</objective>

<execution_context>
@/Users/jjoosshhmbpm1/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jjoosshhmbpm1/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-core-hardening/01-CONTEXT.md
@.planning/phases/01-core-hardening/01-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs -->

From core/types.ts:
```typescript
export interface LLMProvider {
  name: string;
  call(params: LLMCallParams): Promise<LLMResponse>;
  calculateCost(usage: { inputTokens: number; outputTokens: number }, model: string): number;
}

export interface LLMCallParams {
  model: string;
  messages: Message[];
  temperature?: number;
  maxTokens?: number;
  systemPrompt?: string;
}
```

From core/config.ts:
```typescript
export interface BaseFrameworkConfig {
  models: Record<string, string>;
  parameters: Record<string, number | boolean | string>;
  validation?: Record<string, any>;  // CODE-04: change to Record<string, unknown>
  provider?: "anthropic" | "openai" | "openrouter";
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Install TypeScript + create error hierarchy with tests</name>
  <files>package.json, core/errors.ts, test/core/errors.test.ts</files>
  <behavior>
    - Test 1: `new ProviderError(...)` is `instanceof InstitutionalError`
    - Test 2: `new ValidationError(...)` is `instanceof InstitutionalError`
    - Test 3: `new FrameworkError(...)` is `instanceof InstitutionalError`
    - Test 4: error.code is a valid ErrorCode string enum value
    - Test 5: error.context contains frameworkName, agentName, model, prompt fields
    - Test 6: error.cause preserves the original error
    - Test 7: error.name equals the constructor name (e.g., "ProviderError")
    - Test 8: ProviderError requires cause (not optional)
  </behavior>
  <action>
    1. Run `bun add -d typescript` to install TypeScript as devDependency (CODE-01).

    2. Create `core/errors.ts` implementing the locked error hierarchy design:
       - `InstitutionalError extends Error` with: `code: ErrorCode`, `context: ErrorContext`, `options?: ErrorOptions`
       - `ProviderError extends InstitutionalError` — cause is REQUIRED (not optional) per pitfall #5
       - `ValidationError extends InstitutionalError`
       - `FrameworkError extends InstitutionalError`
       - `ErrorCode` const object with string enum pattern: PROVIDER_RATE_LIMITED, PROVIDER_TIMEOUT, PROVIDER_API_ERROR, VALIDATION_MISSING_INPUT, VALIDATION_INVALID_CONFIG, FRAMEWORK_EXECUTION_FAILED
       - `ErrorContext` interface: frameworkName?, agentName?, model?, prompt?
       - Every constructor calls `Object.setPrototypeOf(this, new.target.prototype)` for instanceof safety
       - Every constructor sets `this.name = this.constructor.name`

    3. Create `test/core/errors.test.ts` with tests covering all behaviors above.

    4. Verify: `bun test test/core/errors.test.ts` passes.
    5. Verify: `bun run typecheck` now finds the `tsc` binary (may still have errors from existing code — that's expected, CODE-01 only requires the binary to resolve).
  </action>
  <verify>
    <automated>bun test test/core/errors.test.ts</automated>
  </verify>
  <done>
    - `typescript` appears in devDependencies in package.json
    - `core/errors.ts` exports InstitutionalError, ProviderError, ValidationError, FrameworkError, ErrorCode, ErrorContext
    - All error tests pass: instanceof chain works, cause chaining works, ErrorCode enum works
  </done>
</task>

<task type="auto">
  <name>Task 2: Fix any types in core and verify typecheck</name>
  <files>core/config.ts, core/types.ts</files>
  <action>
    1. In `core/config.ts`: Change `validation?: Record<string, any>` to `validation?: Record<string, unknown>` in `BaseFrameworkConfig`.

    2. In `core/types.ts`: Add `signal?: AbortSignal` to `LLMCallParams` interface (needed by Plan 03 for PROV-07, but defining the contract now avoids later conflicts).

    3. Run `bun run typecheck` and fix any strict-mode errors in core/ files only. The frameworks/ directory may still have errors — those are out of scope for this plan. Focus on making core/ type-clean.

    Note: If typecheck shows errors in frameworks/ files, that is expected and will be addressed in Plan 04 (CODE-03, CODE-06). Do not attempt to fix framework files here.
  </action>
  <verify>
    <automated>bun run typecheck 2>&1 | grep -c "core/" | xargs -I {} test {} -eq 0 && echo "PASS: no core/ errors" || (bun run typecheck 2>&1 | grep "core/" && echo "FAIL: core/ has type errors")</automated>
  </verify>
  <done>
    - `core/config.ts` uses `Record<string, unknown>` not `Record<string, any>`
    - `LLMCallParams` includes optional `signal?: AbortSignal`
    - `bun run typecheck` reports zero errors in core/ files
  </done>
</task>

</tasks>

<verification>
- `bun test test/core/errors.test.ts` passes all tests
- `bun run typecheck` resolves `tsc` binary (typescript installed)
- `core/errors.ts` exports complete error hierarchy
- No `any` types remain in core/ files
</verification>

<success_criteria>
- TypeScript is an explicit devDependency and `bun run typecheck` resolves
- Error hierarchy is implemented with full instanceof chain, cause chaining, and ErrorCode enum
- All error tests pass
- core/ files have zero type errors under strict mode
</success_criteria>

<output>
After completion, create `.planning/phases/01-core-hardening/01-01-SUMMARY.md`
</output>
