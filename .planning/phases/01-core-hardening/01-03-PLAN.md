---
phase: 01-core-hardening
plan: 03
type: execute
wave: 2
depends_on: ["01-01", "01-02"]
files_modified:
  - core/retry.ts
  - core/orchestrator.ts
  - core/providers/anthropic.ts
  - core/providers/openai.ts
  - core/providers/openrouter.ts
  - test/core/providers.test.ts
  - test/core/orchestrator.test.ts
autonomous: true
requirements:
  - PROV-06
  - PROV-07
  - CODE-07

must_haves:
  truths:
    - "Provider calls retry 3 times on 429/500 with exponential backoff (1s, 2s, 4s) before throwing ProviderError"
    - "Provider calls honor Retry-After headers from the server"
    - "Provider calls are aborted after 120s (default) via AbortController"
    - "executeParallel() processes at most N concurrent agents (default 5)"
    - "Concurrency cap is overridable via concurrency parameter"
  artifacts:
    - path: "core/retry.ts"
      provides: "Shared retry/backoff/timeout utility"
      exports: ["withRetry", "parseRetryAfterMs"]
    - path: "core/orchestrator.ts"
      provides: "Semaphore-gated executeParallel and FrameworkRunner"
      exports: ["Semaphore", "executeParallel", "FrameworkRunner"]
  key_links:
    - from: "core/providers/openai.ts"
      to: "core/retry.ts"
      via: "withRetry wrapper around fetch"
      pattern: "withRetry"
    - from: "core/providers/openrouter.ts"
      to: "core/retry.ts"
      via: "withRetry wrapper around fetch"
      pattern: "withRetry"
    - from: "core/providers/anthropic.ts"
      to: "core/retry.ts"
      via: "withRetry wrapper around SDK call"
      pattern: "withRetry"
    - from: "core/orchestrator.ts"
      to: "Semaphore"
      via: "acquire/release around each agent"
      pattern: "sem\\.acquire.*sem\\.release"
---

<objective>
Add retry with exponential backoff, per-agent timeouts via AbortController, and a concurrency semaphore to executeParallel.

Purpose: PROV-06 prevents transient 429/500 errors from failing runs. PROV-07 prevents hung requests from blocking indefinitely. CODE-07 prevents rate limit bursts when frameworks run many agents in parallel.
Output: `core/retry.ts` utility, updated providers and orchestrator, passing tests.
</objective>

<execution_context>
@/Users/jjoosshhmbpm1/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jjoosshhmbpm1/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-core-hardening/01-CONTEXT.md
@.planning/phases/01-core-hardening/01-RESEARCH.md
@.planning/phases/01-core-hardening/01-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 (must exist before this runs) -->

From core/errors.ts (created by Plan 01):
```typescript
export class InstitutionalError extends Error { ... }
export class ProviderError extends InstitutionalError { ... }
export const ErrorCode = {
  PROVIDER_RATE_LIMITED: "PROVIDER_RATE_LIMITED",
  PROVIDER_TIMEOUT: "PROVIDER_TIMEOUT",
  PROVIDER_API_ERROR: "PROVIDER_API_ERROR",
  // ...
} as const;
export interface ErrorContext { frameworkName?, agentName?, model?, prompt? }
```

From core/types.ts (updated by Plan 01):
```typescript
export interface LLMCallParams {
  model: string;
  messages: Message[];
  temperature?: number;
  maxTokens?: number;
  systemPrompt?: string;
  signal?: AbortSignal;  // Added by Plan 01
}
```

From core/orchestrator.ts (current):
```typescript
export async function executeParallel<T>(agents: (() => Promise<T>)[]): Promise<T[]> {
  return Promise.all(agents.map((agent) => agent()));
}

export class FrameworkRunner<TInput, TResult> {
  async runAgent(agentName, provider, model, prompt, temperature?, maxTokens?): Promise<LLMResponse>
  async runParallel(agents: Array<{name, provider, model, prompt, temperature?, maxTokens?}>): Promise<LLMResponse[]>
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create retry/timeout utility and wire into all 3 providers</name>
  <files>core/retry.ts, core/providers/anthropic.ts, core/providers/openai.ts, core/providers/openrouter.ts, test/core/providers.test.ts</files>
  <behavior>
    - Test 1: withRetry retries 3 times on 429 status before throwing ProviderError with PROVIDER_RATE_LIMITED
    - Test 2: withRetry retries on 500 status
    - Test 3: withRetry does NOT retry on 400 (client error)
    - Test 4: withRetry honors Retry-After header (integer seconds format)
    - Test 5: withRetry aborts after timeoutMs and throws ProviderError with PROVIDER_TIMEOUT
    - Test 6: parseRetryAfterMs parses integer seconds and HTTP-date formats
    - Test 7: withRetry uses full jitter on exponential backoff
  </behavior>
  <action>
    1. Create `core/retry.ts` with:
       - `parseRetryAfterMs(header: string | null | undefined): number | null` — handles integer seconds and HTTP-date formats
       - `withRetry<T>(fn: (signal: AbortSignal) => Promise<T>, options?: { maxAttempts?: number, timeoutMs?: number, retryOn?: (error: unknown) => boolean, context?: ErrorContext }): Promise<T>`
         - Default: 3 attempts, 120_000ms timeout, retry on 429/500/503
         - Create fresh AbortController per attempt. If caller provides external signal via `AbortSignal.any()`, combine it.
         - Exponential backoff with full jitter: `delay = Math.random() * baseDelay * 2^(attempt-1)` (per AWS pattern, Claude's discretion)
         - Honor Retry-After header: check `err.headers?.['retry-after']` (Anthropic SDK errors) or `err.response?.headers?.get('retry-after')` (fetch errors)
         - clearTimeout in finally block (prevent timer leak per pitfall #3)
         - On timeout: throw `new ProviderError("Request timed out", ErrorCode.PROVIDER_TIMEOUT, context, { cause: err })`
         - On max retries: throw `new ProviderError("Max retries exceeded", ErrorCode.PROVIDER_RATE_LIMITED, context, { cause: lastError })`
         - Fresh AbortController per retry attempt (per anti-pattern in research)

    2. Update `core/providers/openai.ts` `.call()` method:
       - Wrap the fetch call with `withRetry`
       - Pass `params.signal` (from LLMCallParams) to combine with timeout signal
       - Pass `signal` to `fetch()` options
       - Convert non-OK responses to include status for retry detection

    3. Update `core/providers/openrouter.ts` `.call()` method:
       - Same pattern as OpenAI — wrap fetch with `withRetry`, pass signal

    4. Update `core/providers/anthropic.ts` `.call()` method:
       - Wrap `this.client.messages.create()` with `withRetry`
       - The Anthropic SDK has internal retry for some errors — but we want consistent behavior across all providers, so wrap it anyway
       - Pass AbortSignal via `{ signal }` option to `messages.create()` if the SDK version supports it

    5. Add retry/timeout tests to `test/core/providers.test.ts`:
       - Mock fetch to return 429 twice then 200 — verify 3 calls total
       - Mock fetch to return 429 three times — verify ProviderError thrown
       - Mock fetch to never resolve — verify timeout abort after configured ms
       - Test parseRetryAfterMs with "30" (seconds) and date string
  </action>
  <verify>
    <automated>bun test test/core/providers.test.ts</automated>
  </verify>
  <done>
    - core/retry.ts exports withRetry and parseRetryAfterMs
    - All 3 providers use withRetry for their API calls
    - Retries 3 times on 429/500 with exponential backoff + jitter
    - Honors Retry-After headers
    - Aborts after configurable timeout via AbortController
    - All provider tests pass
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add Semaphore to executeParallel and FrameworkRunner</name>
  <files>core/orchestrator.ts, test/core/orchestrator.test.ts</files>
  <behavior>
    - Test 1: executeParallel with concurrency=2 and 5 agents — at most 2 run simultaneously
    - Test 2: executeParallel with default concurrency (5) processes 5 agents in parallel
    - Test 3: Semaphore acquire/release correctly queues and dequeues waiters
    - Test 4: FrameworkRunner.runParallel uses the shared semaphore
    - Test 5: If one agent in executeParallel fails, others still complete (Promise.allSettled + AggregateError pattern per Claude's discretion: use allSettled, throw AggregateError if any failed, since a committee where half fail is a failed session)
  </behavior>
  <action>
    1. Add `Semaphore` class to `core/orchestrator.ts` (or a new file if orchestrator is getting large):
       - `constructor(permits: number)`
       - `async acquire(): Promise<void>` — decrements or queues
       - `release(): void` — hands to next waiter or increments
       - Per locked decision: ~20-line hand-rolled implementation, no npm dependency

    2. Update `executeParallel`:
       - Add `concurrency` parameter (default 5)
       - Create Semaphore(concurrency)
       - Wrap each agent with acquire/release in try/finally
       - Per Claude's discretion (parallel failure): use `Promise.allSettled`, check for rejected results, throw `AggregateError` wrapping all `ProviderError` instances if any failed. All agents get to finish (no fail-fast).

    3. Update `FrameworkRunner`:
       - Accept optional `concurrency` in constructor or via a setter
       - Store a `Semaphore` instance on the runner (per-run shared semaphore per locked decision)
       - `runParallel()` wraps each agent's `runAgent()` call with the shared semaphore's acquire/release
       - `runAgent()` accepts optional `systemPrompt` parameter (currently missing — agents can't pass system prompts through the runner)

    4. Add new test cases to `test/core/orchestrator.test.ts`:
       - Test concurrent execution with timing: 5 agents that each take 100ms, concurrency=2 — total time should be ~300ms (3 batches), not ~100ms (all parallel)
       - Test that semaphore release happens even on agent failure (try/finally)
  </action>
  <verify>
    <automated>bun test test/core/orchestrator.test.ts</automated>
  </verify>
  <done>
    - Semaphore class exists with acquire/release
    - executeParallel accepts concurrency parameter (default 5)
    - executeParallel gates concurrent execution via semaphore
    - FrameworkRunner holds per-run shared semaphore
    - All orchestrator tests pass including new concurrency tests
  </done>
</task>

</tasks>

<verification>
- `bun test test/core/providers.test.ts` — retry, timeout, backoff tests pass
- `bun test test/core/orchestrator.test.ts` — concurrency cap tests pass
- `bun test test/core` — all core tests pass together
</verification>

<success_criteria>
- PROV-06: All 3 providers retry 3x on 429/500 with exponential backoff + jitter, honor Retry-After
- PROV-07: All providers abort after configurable timeout via AbortController
- CODE-07: executeParallel and FrameworkRunner.runParallel cap concurrency at 5 (configurable)
</success_criteria>

<output>
After completion, create `.planning/phases/01-core-hardening/01-03-SUMMARY.md`
</output>
