---
phase: 01-core-hardening
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - core/providers/openai.ts
  - core/providers/openrouter.ts
  - frameworks/delphi/index.ts
  - frameworks/dissertation-committee/orchestrator.ts
autonomous: true
gap_closure: true
requirements: [CODE-01, CODE-03, CODE-04, CODE-06, CODE-07, PROV-01, PROV-05, PROV-06, PROV-07]

must_haves:
  truths:
    - "`bun run typecheck` exits with code 0 (zero errors)"
    - "All 63 existing tests still pass after the fixes"
  artifacts:
    - path: "core/providers/openai.ts"
      provides: "Typed response from response.json() — no TS18046 errors"
      contains: "interface OpenAIChatResponse"
    - path: "core/providers/openrouter.ts"
      provides: "Typed response from response.json() — no TS18046 errors"
      contains: "interface OpenRouterChatResponse"
    - path: "frameworks/delphi/index.ts"
      provides: "Correct property access via r.statistics.median instead of r.median"
    - path: "frameworks/dissertation-committee/orchestrator.ts"
      provides: "No import/local declaration conflict for formCommittee"
  key_links:
    - from: "core/providers/openai.ts"
      to: "response.json()"
      via: "typed cast to OpenAIChatResponse"
      pattern: "as OpenAIChatResponse"
    - from: "core/providers/openrouter.ts"
      to: "response.json()"
      via: "typed cast to OpenRouterChatResponse"
      pattern: "as OpenRouterChatResponse"
---

<objective>
Fix all 15 remaining TypeScript strict-mode errors so `bun run typecheck` passes with zero errors.

Purpose: Close the single remaining gap from Phase 1 verification — ROADMAP success criterion 1 requires zero typecheck errors.
Output: All 4 files fixed, `bun run typecheck` exits 0.
</objective>

<execution_context>
@/Users/jjoosshhmbpm1/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jjoosshhmbpm1/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-core-hardening/01-VERIFICATION.md

<interfaces>
<!-- The OpenAI and OpenRouter providers both use raw fetch against OpenAI-compatible chat completions API.
     response.json() returns unknown under strict mode. The response shape is identical for both. -->

OpenAI Chat Completions response shape (from API docs):
```typescript
interface OpenAIChatResponse {
  id: string;
  object: string;
  model: string;
  choices: Array<{
    index: number;
    message: {
      role: string;
      content: string;
    };
    finish_reason: string;
  }>;
  usage: {
    prompt_tokens: number;
    completion_tokens: number;
    total_tokens: number;
  };
}
```

From frameworks/delphi/types.ts — RoundSummary has nested statistics:
```typescript
interface RoundSummary {
  round: number;
  estimates: ExpertEstimate[];
  statistics: {
    median: number;
    mean: number;
    range: { min: number; max: number };
    iqr: { q1: number; q3: number };
  };
  convergence: number;
}
```
<!-- Line 164 of delphi/index.ts accesses r.median, r.mean, r.range directly — should be r.statistics.median etc. -->

From frameworks/dissertation-committee/orchestrator.ts:
<!-- Line 3 imports formCommittee from ./committee, but line 98 defines a local function also named formCommittee.
     Fix: rename the local function since the import is the one used in runCommitteeReview (line 22). -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add typed response interfaces to OpenAI and OpenRouter providers</name>
  <files>core/providers/openai.ts, core/providers/openrouter.ts</files>
  <action>
In `core/providers/openai.ts`:
1. Add an `OpenAIChatResponse` interface at the top of the file (after imports, before class) matching the OpenAI chat completions response shape:
   - `id: string`, `object: string`, `model: string`
   - `choices: Array<{ index: number; message: { role: string; content: string }; finish_reason: string }>`
   - `usage: { prompt_tokens: number; completion_tokens: number; total_tokens: number }`
2. On line 59, change `const data = await response.json();` to `const data = await response.json() as OpenAIChatResponse;`
3. No other changes needed — the downstream property accesses (`data.choices[0]`, `data.model`, `data.usage.prompt_tokens`, etc.) will type-check once `data` has the interface type.

In `core/providers/openrouter.ts`:
1. Add the same interface (can be named `OpenRouterChatResponse` or reuse the same shape — OpenRouter uses OpenAI-compatible response format).
2. On line 57, change `const data = await response.json();` to `const data = await response.json() as OpenRouterChatResponse;`

Do NOT extract a shared interface to a separate file — keep each provider self-contained. The duplication is minimal (~10 lines) and avoids cross-file coupling for a simple DTO.
  </action>
  <verify>
    <automated>bun run typecheck 2>&1 | grep -c "core/providers" | xargs -I{} test {} -eq 0 && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>Zero TS18046 errors in core/providers/openai.ts and core/providers/openrouter.ts. `bun run typecheck` reports no errors from these files.</done>
</task>

<task type="auto">
  <name>Task 2: Fix Delphi property access and dissertation-committee naming conflict</name>
  <files>frameworks/delphi/index.ts, frameworks/dissertation-committee/orchestrator.ts</files>
  <action>
In `frameworks/delphi/index.ts`:
1. Line 164 currently reads: `Round ${r.round}: Median=${r.median}, Mean=${r.mean}, Range=${r.range.min}-${r.range.max}, Convergence=${r.convergence.toFixed(3)}`
2. Change to: `Round ${r.round}: Median=${r.statistics.median}, Mean=${r.statistics.mean}, Range=${r.statistics.range.min}-${r.statistics.range.max}, Convergence=${r.convergence.toFixed(3)}`
3. The `RoundSummary` type has `statistics.median`, `statistics.mean`, `statistics.range` — the code was accessing them without the `statistics` prefix.

In `frameworks/dissertation-committee/orchestrator.ts`:
1. Line 3 imports `formCommittee` from `./committee`.
2. Line 98 defines a local function also named `formCommittee`.
3. The import on line 3 is used in `runCommitteeReview` (line 22). The local function on line 98 is a different implementation (hardcoded committee members).
4. Rename the local function on line 98 from `formCommittee` to `formDefaultCommittee` (or remove the import if the local function is the intended one).
5. Looking at the code: line 22 calls `formCommittee(work, config)` — this matches BOTH the import signature and local function signature. The local function (line 98) creates hardcoded committee members. The imported one from `./committee` likely does something more sophisticated. Since the local function shadows the import, TypeScript raises TS2440. The simplest correct fix: rename the local function to `formDefaultCommittee` and do NOT change the call on line 22 (which should use the imported version from `./committee`).
  </action>
  <verify>
    <automated>bun run typecheck 2>&1 | grep -c "frameworks/" | xargs -I{} test {} -eq 0 && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>Zero TS2339 errors in delphi/index.ts. Zero TS2440 errors in dissertation-committee/orchestrator.ts. `bun run typecheck` passes with zero errors total.</done>
</task>

</tasks>

<verification>
```bash
# Full typecheck — must exit 0
bun run typecheck

# All existing tests must still pass
bun test test/core

# Verify no regressions in provider behavior
bun test test/core/providers.test.ts
```
</verification>

<success_criteria>
1. `bun run typecheck` exits with code 0 — zero TypeScript errors
2. All 63 existing tests pass with zero failures
3. No behavioral changes — only type annotations and property path corrections
</success_criteria>

<output>
After completion, create `.planning/phases/01-core-hardening/01-05-SUMMARY.md`
</output>
